# Бот для работы с базой данных

## Структура проекта

Проект рефакторирован и оптимизирован для лучшей производительности и поддержки.

### Структура файлов:

```
oko_bot/
├── bot.py                 # Главный файл запуска бота
├── config.py              # Конфигурация (токены, настройки)
├── states.py              # Состояния FSM для диалогов
├── database/              # Модуль работы с БД
│   ├── __init__.py
│   ├── models.py          # Модели данных (Person, UserLog)
│   └── database.py        # Класс Database с оптимизацией
├── utils/                  # Утилиты
│   ├── __init__.py
│   ├── formatters.py      # Форматирование данных
│   ├── validators.py      # Валидация данных
│   ├── keyboards.py       # Клавиатуры бота
│   └── auth.py            # Управление авторизацией
└── handlers/              # Обработчики команд
    ├── __init__.py
    ├── start.py            # Команда /start
    ├── auth.py             # Авторизация
    ├── search.py           # Поиск записей
    ├── add.py              # Добавление записей
    ├── logs.py             # Просмотр логов (админы)
    └── common.py           # Общие команды
```

## Улучшения

### 1. Оптимизация работы с БД

- **Пул соединений**: Настроен пул соединений с параметрами:
  - `pool_size=5` - размер пула
  - `max_overflow=10` - максимальное количество дополнительных соединений
  - `pool_recycle=3600` - время жизни соединения
  - `pool_pre_ping=True` - проверка соединений перед использованием

- **Индексы БД**: Добавлены индексы для быстрого поиска:
  - Индекс на `fio`, `phone`, `car_number`, `passport`
  - Составные индексы для оптимизации запросов
  - Индекс на `timestamp`, `user_id`, `action` в логах

- **Контекстные менеджеры**: Использование `@contextmanager` для безопасной работы с сессиями

- **Scoped Session**: Использование `scoped_session` для thread-safety

### 2. Рефакторинг кода

- **Модульная структура**: Код разделен на логические модули
- **Разделение ответственности**: Каждый модуль отвечает за свою область
- **Улучшенная валидация**: Централизованная валидация данных
- **Обработка ошибок**: Улучшенная обработка исключений

### 3. Улучшения производительности

- **Оптимизированные запросы**: Использование индексов для быстрого поиска
- **Нормализация данных**: Нормализация телефонов и других данных
- **Кэширование**: Готовность к добавлению кэширования

## Установка и запуск

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` с переменными:
```
BOT_TOKEN=your_bot_token
DATABASE_URL=your_database_url
```

3. Запустите бота:
```bash
python bot.py
```

## Основные функции

- 🔍 **Поиск**: Поиск записей по ФИО, телефону, номеру авто, адресу или паспорту
- ➕ **Добавление**: Пошаговое добавление новых записей
- 📊 **Логи**: Просмотр логов действий (только для админов)
- 🔐 **Авторизация**: Система авторизации с кодами доступа

## Роли пользователей

- **Обычный пользователь** (код: 12345): Поиск и добавление записей
- **Администратор** (код: 77777): Все функции + просмотр логов

## Технологии

- Python 3.11+
- aiogram 2.25.1
- SQLAlchemy 2.0.23
- PostgreSQL (через psycopg2)

## Производительность

После оптимизации:
- ⚡ Быстрее поиск благодаря индексам
- 🔄 Эффективное использование соединений с пулом
- 📈 Масштабируемость для большего количества пользователей
- 🛡️ Безопасная работа с БД через контекстные менеджеры

